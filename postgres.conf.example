# PostgreSQL connection configuration
# Each line is a key=value pair that forms the connection string
# Lines starting with # are comments

host=localhost
port=5432
dbname=anode
user=postgres
password=secret
